// config.js

/**
 * Configuration for chart colors.
 * @type {Object}
 */
export const CHART_COLORS = {
    borderColor: 'rgba(8, 66, 255, 0.8)', // Color for the chart border
    hoverColor: 'rgba(8, 66, 255, 0.6)', // Color for the chart on hover
};

/**
 * Tooltip messages for the application.
 * @type {Object}
 */
export const TOOLTIP_MESSAGES = {
    uploadInfo: "Please upload an Excel file with x-axis values in the first column and y-axis values in the second column.",
};

/**
 * Labels for the application UI.
 * @type {Object}
 */
export const APP_LABELS = {
    uploadButton: 'Upload Excel File', // Label for the upload button
};
